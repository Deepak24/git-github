/**
 * Description: Function return 
 */

function calculateAdd(x, y){
    return x + y;
}

console.log("The addition is ", calculateAdd(4, 5));

//Another way of calling
const intResult = calculateAdd(12, 15);
console.log(intResult);